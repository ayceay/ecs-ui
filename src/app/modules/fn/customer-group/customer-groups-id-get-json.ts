/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CustomerGroup } from '../../models/customer-group';

export interface CustomerGroupsIdGet$Json$Params {
  id: string;
  authorization?: string;
}

export function customerGroupsIdGet$Json(http: HttpClient, rootUrl: string, params: CustomerGroupsIdGet$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<CustomerGroup>> {
  const rb = new RequestBuilder(rootUrl, customerGroupsIdGet$Json.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
    rb.header('authorization', params.authorization, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CustomerGroup>;
    })
  );
}

customerGroupsIdGet$Json.PATH = '/customer-groups/{id}';
