/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CustomerGroup } from '../../models/customer-group';

export interface CustomerGroupsPost$Xml$Params {
  authorization?: string;
      body: CustomerGroup
}

export function customerGroupsPost$Xml(http: HttpClient, rootUrl: string, params: CustomerGroupsPost$Xml$Params, context?: HttpContext): Observable<StrictHttpResponse<CustomerGroup>> {
  const rb = new RequestBuilder(rootUrl, customerGroupsPost$Xml.PATH, 'post');
  if (params) {
    rb.header('authorization', params.authorization, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: 'application/xml', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CustomerGroup>;
    })
  );
}

customerGroupsPost$Xml.PATH = '/customer-groups/';
