/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CustomerPaginationModel } from '../../models/customer-pagination-model';
import { CustomerQuery } from '../../models/customer-query';

export interface CustomersQueryPagePost$Xml$Params {
  authorization?: string;
      body: CustomerQuery
}

export function customersQueryPagePost$Xml(http: HttpClient, rootUrl: string, params: CustomersQueryPagePost$Xml$Params, context?: HttpContext): Observable<StrictHttpResponse<CustomerPaginationModel>> {
  const rb = new RequestBuilder(rootUrl, customersQueryPagePost$Xml.PATH, 'post');
  if (params) {
    rb.header('authorization', params.authorization, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: 'application/xml', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CustomerPaginationModel>;
    })
  );
}

customersQueryPagePost$Xml.PATH = '/customers/queryPage';
