/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FindAll } from '../../models/find-all';
import { Query } from '../../models/query';

export interface UsersQueryPagePost$Xml$Params {
  authorization?: string;
      body: Query
}

export function usersQueryPagePost$Xml(http: HttpClient, rootUrl: string, params: UsersQueryPagePost$Xml$Params, context?: HttpContext): Observable<StrictHttpResponse<FindAll>> {
  const rb = new RequestBuilder(rootUrl, usersQueryPagePost$Xml.PATH, 'post');
  if (params) {
    rb.header('authorization', params.authorization, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: 'application/xml', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<FindAll>;
    })
  );
}

usersQueryPagePost$Xml.PATH = '/users/queryPage';
